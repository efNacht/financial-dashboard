services:
  - type: web
    name: financial-dashboard
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "8080"
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
